# Experiments

Configuration files, scripts, and logs for running and tracking machine learning experiments.

- `configs/`: Experiment configuration files (YAML, JSON, etc.).
- `scripts/`: Scripts for running experiments.
- `logs/`: Output logs from experiment runs.