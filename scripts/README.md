# Standalone Scripts

Command-line scripts for training models, evaluating results, and generating outputs.

- `train_model.py`: Script to train models on provided data.
- `evaluate_model.py`: Script to evaluate trained models.
- `generate_results.py`: Script to generate figures and tables for the thesis.